package org.leetCode.Algorithms;

import java.util.List;

public class LC_0119_PascalsTriangleII {
	/*
	 * Given a non-negative index k where k ≤ 33, return the kth index row of the Pascal's triangle.
	 * Note that the row index starts from 0.
	 * 
	 * Example:
	 * Input: 3
	 * Output: [1,3,3,1]
	 * */
	
	public static void main(String[] args) {
		int rowIndex = 5;
		System.out.println(getRow(rowIndex));
	}
	
	/*
	 * Runtime: ?? ms, faster than ??% of Java online submissions for Pascal's Triangle II.
	 * Memory Usage: ?? MB, less than ??% of Java online submissions
	 * for Pascal's Triangle II.
	 */
	
	public static List<Integer> getRow(int rowIndex) {
		return null;
	}
}
